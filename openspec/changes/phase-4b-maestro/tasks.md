# Tasks: Phase 4b — Maestro

- [ ] 1. Create `internal/maestro/maestro.go` with `Client`, `NewFromConfig`, resource types, and all API methods
- [ ] 2. Create `internal/maestro/maestro_test.go` with `httptest.NewServer`-based unit tests
- [ ] 3. Extend `cmd/maestro.go` with list, get, delete, bundles, consumers subcommands
- [ ] 4. Create `cmd/maestro_test.go` with command-level tests
- [ ] 5. Run `go build ./...`, `go vet ./...`, `go test ./...` and save to `verification_proof/`
- [ ] 6. Commit verification proof
