# Kubernetes Utilities — Delta Spec

This spec is a delta against `openspec/specs/kubernetes/spec.md`.

No requirement changes. This change implements the existing spec without modifying requirements.
