# Delta for config-registry

> **ARCHIVED** — This document was a delta/change specification for the YAML storage model. It has been superseded by `config-model/spec.md`, which is the authoritative specification for configuration storage, environment files, and the precedence chain. Do not use this document.
